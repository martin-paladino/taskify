const TaskItem = ({ task }) => {
//task is an object with the following properties:
//id, title, completed, endaDate, image.

    return (
        <div>
        </div>
    );
};

export default TaskItem;